/**
 * Main Types Index - UNA Hotel System
 * 
 * Central export point for all application types.
 * Import from here to ensure you're using the latest consolidated types.
 */

// Core consolidated types (main source of truth)
export * from './core';

// Legacy compatibility exports (will be deprecated)
export * from './guest';

// Note: .d.ts files are ambient module declarations and don't need to be exported
