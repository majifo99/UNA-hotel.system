# Environment Configuration for UNA Hotel System

# API Configuration
# Set to 'true' to use real API endpoints, 'false' for mock data
VITE_USE_REAL_API=false

# API Base URL (only used when VITE_USE_REAL_API=true)
# Replace with your actual API endpoint
VITE_API_BASE_URL=http://localhost:3001/api

# Development vs Production
NODE_ENV=development

# Optional: Authentication token for API requests
# This would typically be set by your authentication system
# VITE_AUTH_TOKEN=your-jwt-token-here
