# Configuración para acceder al sistema administrativo
VITE_MODE=admin

# =============================================================================
# API Configuration - Laravel Backend
# =============================================================================
# 
# IMPORTANT: This application requires different configuration for development and production
#
# FOR DEVELOPMENT (local with Vite dev server):
# -----------------------------------------------
# Option 1: Use Vite proxy (recommended for development)
#   - Leave VITE_API_URL commented out or remove it
#   - Set VITE_BACKEND_URL to your local backend
#   - The Vite dev server will proxy /api requests to VITE_BACKEND_URL
VITE_BACKEND_URL=http://localhost:8000

# Option 2: Direct backend connection (skip Vite proxy)
#   - Uncomment and set VITE_API_URL to your backend URL
#   - This will bypass the Vite proxy
# VITE_API_URL=http://localhost:8000/api

# FOR PRODUCTION (Vercel, Netlify, etc.):
# ----------------------------------------
# REQUIRED: You MUST set VITE_API_URL to your production backend URL
# The application will fail to start if this is not set in production
# 
# Example for Render backend:
# VITE_API_URL=https://backendhotelt.onrender.com/api
#
# Example for other backends:
# VITE_API_URL=https://api.yourhotel.com/api
#
# NOTE: Vercel does not provide automatic API proxying like Vite dev server.
#       You must provide the full backend URL.

# =============================================================================
# reCAPTCHA v3 Configuration
# =============================================================================
# Site key for reCAPTCHA v3 (public key)
# Get your keys at: https://www.google.com/recaptcha/admin
VITE_RECAPTCHA_SITE_KEY=your_recaptcha_site_key_here
