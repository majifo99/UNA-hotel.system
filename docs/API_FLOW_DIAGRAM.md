# API Request Flow: Development vs Production

## Development Environment (with Vite Dev Server)

```
┌─────────────────────────────────────────────────────────────┐
│ Developer's Machine                                          │
│                                                              │
│  ┌──────────────┐                                           │
│  │   Browser    │                                           │
│  │  Frontend    │                                           │
│  └──────┬───────┘                                           │
│         │                                                    │
│         │ 1. Request to /api/users                          │
│         ↓                                                    │
│  ┌──────────────────────┐                                   │
│  │  Vite Dev Server     │                                   │
│  │  (localhost:5173)    │                                   │
│  │                      │                                   │
│  │  Proxy Config:       │                                   │
│  │  /api → Backend URL  │                                   │
│  └──────┬───────────────┘                                   │
│         │                                                    │
│         │ 2. Proxies to http://localhost:8000/api/users    │
│         ↓                                                    │
│  ┌──────────────────────┐                                   │
│  │  Laravel Backend     │                                   │
│  │  (localhost:8000)    │                                   │
│  │                      │                                   │
│  │  ✅ Request arrives  │                                   │
│  │  ✅ Response sent    │                                   │
│  └──────────────────────┘                                   │
│                                                              │
└──────────────────────────────────────────────────────────────┘

Environment Variables:
  VITE_BACKEND_URL=http://localhost:8000  (for Vite proxy)
  VITE_API_URL=(not set, uses '/api' fallback)

Result: ✅ Works perfectly - no CORS issues
```

## Production Environment (Vercel) - BEFORE FIX ❌

```
┌─────────────────────────────────────────────────────────────┐
│ User's Browser                                               │
│                                                              │
│  ┌──────────────┐                                           │
│  │   Browser    │                                           │
│  │  Frontend    │                                           │
│  └──────┬───────┘                                           │
│         │                                                    │
│         │ 1. Request to /api/users                          │
│         ↓                                                    │
│  ┌──────────────────────────────────────┐                   │
│  │  Vercel Static Server                │                   │
│  │  (yourapp.vercel.app)                │                   │
│  │                                      │                   │
│  │  ❌ NO PROXY AVAILABLE               │                   │
│  │  ❌ Tries same origin:               │                   │
│  │     https://yourapp.vercel.app/api   │                   │
│  │                                      │                   │
│  │  Result: 404 Not Found               │                   │
│  └──────────────────────────────────────┘                   │
│                                                              │
└──────────────────────────────────────────────────────────────┘

Environment Variables:
  VITE_API_URL=(not set)
  
Code fallback: uses '/api'

Result: ❌ FAILS - API endpoints don't exist on Vercel
```

## Production Environment (Vercel) - AFTER FIX ✅

```
┌─────────────────────────────────────────────────────────────┐
│ User's Browser                                               │
│                                                              │
│  ┌──────────────┐                                           │
│  │   Browser    │                                           │
│  │  Frontend    │                                           │
│  └──────┬───────┘                                           │
│         │                                                    │
│         │ 1. Request to full backend URL                    │
│         │    https://backendhotelt.onrender.com/api/users  │
│         │                                                    │
│         └────────────────┐                                   │
│                          │                                   │
└──────────────────────────┼───────────────────────────────────┘
                           │
                           │ 2. Direct request to backend
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ Backend Server (Render)                                      │
│                                                              │
│  ┌──────────────────────────────────┐                       │
│  │  Laravel Backend                 │                       │
│  │  (backendhotelt.onrender.com)    │                       │
│  │                                  │                       │
│  │  ✅ Request arrives              │                       │
│  │  ✅ CORS allows frontend domain  │                       │
│  │  ✅ Response sent                │                       │
│  └──────────────────────────────────┘                       │
│                                                              │
└──────────────────────────────────────────────────────────────┘

Environment Variables:
  VITE_API_URL=https://backendhotelt.onrender.com/api ✅

Result: ✅ Works - direct backend connection
```

## Configuration Logic Flow

```
┌─────────────────────────────────────────────────────────┐
│ getApiBaseUrl() function                                 │
│                                                          │
│  ┌──────────────────────────────────┐                   │
│  │ Is VITE_API_URL set?             │                   │
│  └──────┬───────────────────┬───────┘                   │
│         │ YES               │ NO                         │
│         ↓                   ↓                            │
│  ┌──────────────┐    ┌─────────────────┐               │
│  │ Return       │    │ Is DEV mode?    │               │
│  │ VITE_API_URL │    └────┬──────┬─────┘               │
│  └──────────────┘         │ YES  │ NO                   │
│                           ↓      ↓                       │
│                    ┌─────────┐ ┌────────────────┐       │
│                    │ Return  │ │ Throw Error    │       │
│                    │ '/api'  │ │ "VITE_API_URL  │       │
│                    │         │ │  required"     │       │
│                    └─────────┘ └────────────────┘       │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

## Service Integration Flow

```
┌──────────────────────────────────────────────────────────────┐
│ Service Files (Before)                                        │
│                                                               │
│  BaseApiService.ts      ─┐                                   │
│  httpClient.ts          ─┤  Each with own                    │
│  apiService.ts          ─┤  URL logic                        │
│  apiClient.ts           ─┤  (12+ files)                      │
│  MultiHttpClient.ts     ─┤  ❌ Inconsistent                  │
│  limpiezaService.ts     ─┤  ❌ Duplicated                    │
│  maintenanceService.ts  ─┤  ❌ Error-prone                   │
│  guestApiService.ts     ─┘                                   │
│                                                               │
└───────────────────────────────────────────────────────────────┘

                            ↓
                     [REFACTORED]
                            ↓

┌──────────────────────────────────────────────────────────────┐
│ Service Files (After)                                         │
│                                                               │
│  ┌────────────────────────────────┐                          │
│  │  src/config/api.ts             │                          │
│  │  Single Source of Truth        │                          │
│  │  ✅ getApiBaseUrl()             │                          │
│  │  ✅ validateApiConfig()         │                          │
│  └────────────┬───────────────────┘                          │
│               │                                               │
│               │ import { getApiBaseUrl }                      │
│               │                                               │
│  ┌────────────┴───────────────────┐                          │
│  │                                 │                          │
│  ├── BaseApiService.ts            │                          │
│  ├── httpClient.ts                │  All use                 │
│  ├── apiService.ts                │  same config             │
│  ├── apiClient.ts                 │  ✅ Consistent           │
│  ├── MultiHttpClient.ts           │  ✅ Maintainable         │
│  ├── limpiezaService.ts           │  ✅ Reliable             │
│  ├── maintenanceService.ts        │                          │
│  └── guestApiService.ts           │                          │
│                                                               │
└───────────────────────────────────────────────────────────────┘
```

## Error Handling Flow

### Before Fix
```
App Start
   ↓
UI Loads ✅
   ↓
User Clicks Button
   ↓
API Call: fetch('/api/users')
   ↓
❌ 404 Not Found
   ↓
Cryptic Error Message
   ↓
😞 Bad User Experience
```

### After Fix
```
App Start
   ↓
Validate Config: getApiBaseUrl()
   ↓
┌─────────────────┐
│ Config Valid?   │
└────┬──────┬─────┘
     │ YES  │ NO
     ↓      ↓
  ┌────┐ ┌─────────────────────┐
  │ ✅ │ │ Show Error Page:    │
  │    │ │ "VITE_API_URL       │
  │    │ │  environment        │
  │    │ │  variable required" │
  └────┘ └─────────────────────┘
     │
     ↓
  UI Loads
     ↓
  API Calls Work ✅
     ↓
  😊 Good User Experience
```

## Summary

### The Problem
- Development: Vite provides `/api` proxy ✅
- Production: Vercel has no proxy ❌
- Result: Same code, different behavior

### The Solution
1. Detect environment (dev vs prod)
2. Require explicit config in production
3. Use consistent configuration everywhere
4. Validate early, fail fast
5. Show clear error messages

### Result
- Development: Works as before ✅
- Production: Works reliably ✅
- Errors: Caught early with clear messages ✅
- Maintenance: Single source of truth ✅
